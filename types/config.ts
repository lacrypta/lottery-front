export interface Config {
  totalPlayers: number;
  blockTarget: number;
  getBlockApiKey: string;
  totalWinners: number;
  staggeringDelay: number;
  lotteryDelay: number;
  contractAddress: string;
}
